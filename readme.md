```js
/**
 * index.js: Manages the scraper API, Manages input data and outputs required data
 * browser.js: various scraping modes
 * helpers:
 *  - node: modules required to run in node context
 *  - browser: modules required to run in browser context, compiled by webpack if need
 */
```